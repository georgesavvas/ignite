#!/bin/bash

# echo "Killing processes listening on 9070...";
# lsof -P | grep ':9070' | awk '{print $2}' | xargs kill -9
parent_path=$( cd "$(dirname "${BASH_SOURCE[0]}")" ; pwd -P )
cd "$parent_path"
cd ../../..
source env/bin/activate
cd src/python
python3 -m ignite.main
